@{
    Layout = "_Layout";
}

@model List<TicketingSystemFightNight.Models.TicketShopModels>

<nav class="breadcrumbs">
    <a asp-controller="Home" asp-action="Index">Početna</a> /
    <span>Ponude karata</span>
</nav>

<h2>Pregled ponuda karata</h2>

@if (TempData["SuccessMessage"] != null)
{
    <div class="alert alert-success" role="alert">
        @TempData["SuccessMessage"]
    </div>
}

<div class="card">
    <div class="card-body">
        <a asp-action="Create" class="btn btn-primary mb-3">
            <i class="bi bi-plus-circle"></i> Dodaj ponudu
        </a>

        <div class="table-responsive">
            <table class="table table-hover align-middle">
                <thead>
                    <tr>
                        <th>Id</th>
                        <th>Naslov</th>
                        <th>Događaj</th>
                        <th>Lokacija</th>
                        <th>Cijena</th>
                        <th>VIP</th>
                        <th>Akcije</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var offer in Model)
                    {
                        <tr>
                            <td>@offer.Id</td>
                            <td>@offer.Title</td>
                            <td>@offer.EventName</td>
                            <td>@offer.Location</td>
                            <td>@offer.Price.ToString("C")</td>
                            <td>@(offer.IsVip ? "Da" : "Ne")</td>
                            <td>
                                <a class="btn btn-sm btn-outline-secondary me-2" asp-action="Edit" asp-route-id="@offer.Id">
                                    <i class="bi bi-pencil"></i> Uredi
                                </a>
                                <form asp-action="Delete" asp-route-id="@offer.Id" method="post" class="d-inline" onsubmit="return confirm('Jeste li sigurni da želite obrisati ovu ponudu?');">
                                    <button type="submit" class="btn btn-sm btn-outline-danger">
                                        <i class="bi bi-trash"></i> Obriši
                                    </button>
                                </form>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>
